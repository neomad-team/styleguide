machine:
  node:
    version: stable
    
general:
  branches:
    ignore:
      - dist

dependencies:
  override:
    - npm install
  cache_directories:
    - node_modules

test:
  override:
    - npm run lint
